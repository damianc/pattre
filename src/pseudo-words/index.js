import { block } from '@pseudo-words/block';
import { group } from '@pseudo-words/group';
import { oneOf } from '@pseudo-words/one-of';

const pseudoWordsAPI = {
  block,
  group,
  oneOf
};

export default pseudoWordsAPI;